/.metadata/

# Eclipse / IDE (jamais versionner)
**/.metadata/

# Données Kaggle / CSV volumineux (limite GitHub 100 Mo — ne pas versionner)
bnpl-data-pipeline/data/application_train.csv
bnpl-data-pipeline/out/*.csv

# Maven — gestion-demande (build local)
gestion-demande/**/target/
